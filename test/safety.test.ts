import {describe,it,expect} from 'vitest';import fs from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {safePath,parseCommand} from '../src/safety.js';
describe('path safety',()=>{it('accepts child and blocks traversal, Windows absolute, and POSIX absolute',async()=>{const root=await fs.mkdtemp(path.join(os.tmpdir(),'fa-'));expect(await safePath(root,'src/a.ts',true)).toContain(root);await expect(safePath(root,'../../outside.txt',true)).rejects.toThrow();await expect(safePath(root,'C:\\outside.txt',true)).rejects.toThrow();await expect(safePath(root,'/outside.txt',true)).rejects.toThrow()})});describe('commands',()=>{it('tokenizes without a shell',()=>expect(parseCommand('npm run build')).toEqual({file:'npm',args:['run','build']}));it.each(['npm install && whoami','node x | sh','sudo npm i','node x > out'])('rejects %s',x=>expect(()=>parseCommand(x)).toThrow())});
